import type { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig = {
  appId: 'com.nuvora.invite',
  appName: 'Nuvora',
  webDir: 'dist',
  server: { androidScheme: 'https' }
}
export default config
